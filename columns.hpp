/**
 * @file
 * @brief Enthält die Deklaration der Spalten-Nummern.
 * @author Björn Schäpers
 */

#ifndef COLUMNS_HPP
#define COLUMNS_HPP

enum class Column {
    Image,
    FirstName,
    LastName,
    DateOfBirth,
    Mail,
    Vip,
    Count,
};

#endif //COLUMNS_HPP
